# Venice

Default base URL: `https://api.venice.ai/api/v1`

Set `VENICE_API_KEY` before using LLM-backed memory operations. Venice is OpenAI-compatible, so Deja Vu uses the OpenAI client with a Venice base URL and removes null request fields before dispatch.
