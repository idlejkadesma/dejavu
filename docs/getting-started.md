# Deja Vu Getting Started

```bash
pip install dejavu-memory
dejavu init
export VENICE_API_KEY=your-key
dejavu serve
```

Deja Vu stores config at `~/.dejavu/config.json` and local memory history at `~/.dejavu/memories.db`.
